import 'package:flutter/material.dart';

class AmenityModel {
  int? id;
  Icon? icon;
  String? title;
  bool? isSelected;

  AmenityModel({
    this.id,
    this.icon,
    this.title,
    this.isSelected,
  });
}
